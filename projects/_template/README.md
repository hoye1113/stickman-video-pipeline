# 兼容性说明 (Legacy Template Notice)

> **注意 (Notice)**：
> 本目录 (`projects/_template`) 为历史向后兼容保留。
> 火柴人视频题材模板的官方单一事实来源 (Single Source of Truth) 已迁移至：
> `presets/stickman/template/`
> 
> 在新架构下，脚手架脚本 `python scripts/new_project.py --genre <genre>` 会自动优先从 `presets/<genre>/template/` 读取模板。
> 本目录仅在未指定题材且 preset 缺失时的底层降级保护，未来版本可能会被软链接（Junction）替换或废弃。
